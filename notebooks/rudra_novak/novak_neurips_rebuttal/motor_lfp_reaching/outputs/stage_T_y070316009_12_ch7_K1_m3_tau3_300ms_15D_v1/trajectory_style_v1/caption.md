The same observed clouds and saved annular fits as the original six-stage panels, rendered with continuous 1-point, opaque, stage-colored trajectories instead of dot markers. Every trajectory joins the 294 observed samples in temporal order without interpolation, smoothing, resampling, or changes to coordinates. The fitted outer solid and inner dashed boundaries are unchanged. Stage colors, grid-free axes, limits, viewpoints, and example trials (6, 9, 10) are unchanged. These lines are not AR-predicted signals. No fitting, feature extraction, or decoding was run. Earlier outputs are preserved.
